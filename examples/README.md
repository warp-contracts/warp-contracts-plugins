# Warp Contracts plugins examples

Examples of `warp-contracts` SDK plugins usage.

In order to run examples
1. `yarn install`
2. use `ts-node` to run examples, e.g. `yarn ts-node subscription/contract.ts`
